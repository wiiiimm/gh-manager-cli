import React from 'react';
import { render } from 'ink';
import RepoList from './dist/index.js';

// Test with a mock token to verify rendering
const testToken = 'test_token';

// This will test the final built version
console.log('Testing RepoList rendering with mock token...');